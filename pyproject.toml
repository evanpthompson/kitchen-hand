[project]
name = "stirfry-companion"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "jsonschema>=4.26.0",
    "markitdown[youtube-transcription]>=0.1.6",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "ruff>=0.15.20",
]
